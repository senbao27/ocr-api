version: 1.0
runtime: docker
build:
  dockerfilePath: Dockerfile
  context: .
run:
  network:
    port: 8000
  healthCheck:
    path: /health
    interval: 10
    timeout: 5
    healthyThreshold: 1
    unhealthyThreshold: 5
